{
  "name": "midnight_otter_blockchain",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install pnpm": "npm install -g pnpm",
    "update pnpm package": "pnpm update",
    "hardhat clean": "npx hardhat clean",
    "hardhat compile": "npx hardhat compile",
    "hardhat wipe": "npx hardhat ignition wipe deploymentId futureId",
    "deploy --network sepolia": "npx hardhat ignition deploy ignition/modules/midnightOtter.js --network sepolia",
    "export artifacts": "node scripts/exportArtifact.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.5",
    "@nomicfoundation/hardhat-ignition": "^0.11.2",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts": "^5.0.1",
    "ethers": "^6.9.0",
    "hardhat": "^2.19.2",
    "hardhat-ethernal": "^3.2.5"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
