import {CubeSpinner} from "@/components/pageComponents/spinnerLoadingComponent";

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <CubeSpinner />;
}
